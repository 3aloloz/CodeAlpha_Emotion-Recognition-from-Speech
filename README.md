# CodeAlpha_Emotion-Recognition-from-Speech
Speech Emotion Recognition using Deep Learning  This project is a Deep Learning-based Speech Emotion Recognition (SER) system that classifies human emotions from audio speech signals using the RAVDESS dataset.  The model processes raw audio files and extracts Log-Mel Spectrogram features, then uses a Convolutional Neural Network (CNN).
